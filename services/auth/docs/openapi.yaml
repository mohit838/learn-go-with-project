openapi: 3.0.3
info:
  title: Auth Service API
  version: 1.0.0
  description: HTTP API exposed by the authentication service.
servers:
  - url: http://localhost:8484
    description: Local development
paths:
  /:
    get:
      summary: Check service health
      operationId: getHealth
      responses:
        '200':
          description: Service is running.
          content:
            text/plain:
              schema:
                type: string
              example: Auth Service API is running!
  /logs:
    post:
      summary: Create an audit log entry
      operationId: createAuditLog
      responses:
        '200':
          description: Audit log created.
          content:
            text/plain:
              schema:
                type: string
              example: Log created!
        '500':
          $ref: '#/components/responses/InternalServerError'
  /cache:
    post:
      summary: Set the default cache value
      description: Stores `test_value` under `test_key` for one hour.
      operationId: setCache
      responses:
        '200':
          description: Cache value set.
          content:
            text/plain:
              schema:
                type: string
              example: Cache set!
        '500':
          $ref: '#/components/responses/InternalServerError'
  /cache/{key}:
    get:
      summary: Get a cached value
      operationId: getCacheValue
      parameters:
        - $ref: '#/components/parameters/CacheKey'
      responses:
        '200':
          description: Cached value.
          content:
            text/plain:
              schema:
                type: string
        '404':
          description: Cache key was not found.
          content:
            text/plain:
              schema:
                type: string
components:
  parameters:
    CacheKey:
      name: key
      in: path
      required: true
      description: Cache key to retrieve.
      schema:
        type: string
      example: test_key
  responses:
    InternalServerError:
      description: A backing service failed to complete the request.
      content:
        text/plain:
          schema:
            type: string
